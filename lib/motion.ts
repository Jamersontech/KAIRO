// Shared motion vocabulary — one easing, one viewport config, everywhere.
export const EASE = [0.22, 1, 0.36, 1] as [number, number, number, number];

export const VIEWPORT = { once: true, margin: "-80px" } as const;
